To run each program:
"java ./Server.java {port}"
"java ./Client.java {port}"

Start the server first before connecting with the client. Server can only handle one connection at a time.

The log file is created wherever the server is located.